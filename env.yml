name: ts-cpu
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip==22.3.1
  - pip:
      - jaxlib==0.4.14
      - jax==0.4.14
      - jaxopt
      - numpy
      - scipy
      - matplotlib
      - pyhdf
      - xlrd
      - pyyaml
      - mlflow
      - boto3
      - flatten-dict
      - dm-haiku==0.0.10
      - typing-extensions
      - optax
      - tqdm
      - xarray