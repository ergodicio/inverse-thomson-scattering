name: tsadar-cpu
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - jaxlib==0.4.14
      - jax==0.4.14
      - jaxopt
      - numpy
      - scipy
      - matplotlib
      - pyhdf
      - xlrd
      - pyyaml
      - mlflow
      - boto3
      - flatten-dict
      - typing-extensions
      - optax
      - tqdm
      - xarray